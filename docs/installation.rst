============
Installation
============

At the command line::

    $ easy_install django-yekpay

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-yekpay
    $ pip install django-yekpay
